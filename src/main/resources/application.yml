##Server Settings
server:
  port: 8080
#  port: ${PORT}

spring:
  datasource:
    url: jdbc:postgresql://pg-c6psj.vpc-pub-cdb-kr.ntruss.com:5432/postgres
    username: admin
    password: postgres123!
    driver-class-name: org.postgresql.Driver
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace