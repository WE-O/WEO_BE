##Server Settings
server:
  port: 8080
#  port: ${PORT}

spring:
  datasource:
    url: jdbc:postgresql://118.67.135.249:5432/postgres
    username: postgres
    password: siksikmulmul123!
    driver-class-name: org.postgresql.Driver
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        #show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace